# Linux / Zorin — Phase 2

等待第一阶段视觉确认。此目录尚无可安装 Xcursor 主题。

后续从 `../assets/states/` 与 `../assets/manifest.json` 构建独立的 CatPaw-Pink / CatPaw-Coffee 目录，提供 `cursors/`、`index.theme`、传统状态别名、Busy 动画及 24–128px 热点。随后在 Zorin / GNOME、X11 和允许使用自定义光标的 Wayland 桌面验证。不得覆盖 IceGem。
